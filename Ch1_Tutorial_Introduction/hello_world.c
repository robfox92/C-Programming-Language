#include<stdio.h>
// C does not require a return type, but C++ does
// if C is given a function without a return type it assumes `int`
// better to be explicit though
int main()
{
printf("hello, world!\n");
}
